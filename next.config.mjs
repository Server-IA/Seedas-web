/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'standalone',
};
/**  module.exports = {
  images: {
    remotePatterns: [
      {
        protocol: 'https',
        hostname: 'assets.example.com',
        port: '',
        pathname: '/account123/**',
      },
    ],
  },        seguridad en la ruta de imagenes y acceso 
} 

*/
export default nextConfig;
